{
    "name": "docker-complete",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "author": "Maximilian Schwarzmüller / Academind GmbH",
    "license": "MIT",
    "dependencies": {
        "express": "^4.17.1",
        "body-parser": "1.19.0"
    },
    "scripts": {
        "start": "node server.js",
        "build-image": "docker build -t section-two-image .",
        "start-container": "docker run -d -p 8080:8080 --rm --name ${CONTAINER_NAME:-section-two-container} section-two-image",
        "stop-container": "docker stop ${CONTAINER_NAME:-section-two-container} || true",
        "docker:clean-start": "npm run stop-container && npm run build-image && npm run start-container"
    }
}
